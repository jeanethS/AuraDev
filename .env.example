ANTHROPIC_API_KEY=your_api_key_here
LYRIA_PROJECT_ID=your_gcp_project_id
# Optional: path to a GCP service account JSON key (otherwise use gcloud ADC)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Comma-separated list of allowed CORS origins (defaults to localhost only)
# CORS_ORIGINS=https://auradev-y1bp.onrender.com,http://localhost:8765